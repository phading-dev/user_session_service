apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-session-service-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-session-service-pod
  template:
    metadata:
      labels:
        app: user-session-service-pod
    spec:
      serviceAccountName: user-session-service-account
      containers:
      - name: user-session-service-container
        image: gcr.io/phading-prod/user-session-service:latest
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /readiness
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
        resources:
          requests:
            cpu: "300m"
            memory: "512Mi"
          limits:
            cpu: "300m"
            memory: "512Mi"
---
apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: user-session-service-monitoring
spec:
  selector:
    matchLabels:
      app: user-session-service-pod
  endpoints:
  - port: 8080
    path: /metricsz
    interval: 30s
---
apiVersion: v1
kind: Service
metadata:
  name: user-session-service
spec:
  selector:
    app: user-session-service-pod
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
---
apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: user-session-service-lb-health-check
spec:
  default:
    config:
      type: HTTP
      httpHealthCheck:
        port: 8080
        requestPath: /healthz
  targetRef:
    group: ""
    kind: Service
    name: user-session-service
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: user-session-service-route-internal
spec:
  parentRefs:
  - name: phading-gateway-internal
    sectionName: http
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /usn
    backendRefs:
    - name: user-session-service
      port: 8080
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: user-session-service-route-external
spec:
  parentRefs:
  - name: phading-gateway-external
    sectionName: https
  hostnames:
  - www.secount.com
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /usw
    backendRefs:
    - name: user-session-service
      port: 8080
